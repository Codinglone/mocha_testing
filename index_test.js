const assert = require('assert');


describe('Sum', () => {
    it('checks if the sum is equal to 7', () => {
        // Setup
        const sum = 7;

        // Exercise
        assert.equal(sum, 7);
    })
})